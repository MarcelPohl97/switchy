import styled from 'styled-components';

const SubHeader = styled.p`
    color: #9A9CB2;
    margin: 1rem 0;
    font-size: 1.25rem;
    line-height: 1.4;
    font-weight:400;
    text-align: center;

    &.small {
        font-size: 1rem;
        line-height: 1.2;
    }
`;

export default SubHeader;